{
  "scripts": {
    "oxypogon": "$(npm bin)/oxypogon",
    "build": "rm -rf build/*; cp -R static build/; npm run oxypogon -- build",
    "publish": "npm run build; ./publish.sh",
    "watch": "npm run oxypogon -- watch"
  },
  "devDependencies": {
    "babel-cli": "^6.23.0",
    "babel-preset-env": "^1.2.1",
    "babel-preset-react": "^6.23.0"
  },
  "dependencies": {
    "react": "^15.4.2",
    "react-dom": "^15.4.2"
  }
}
